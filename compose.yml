# Generated by setupmc.com on 2025-02-02T13:16:25.601Z

services:
  mc:
    image: itzg/minecraft-server:latest
    tty: true
    stdin_open: true
    ports:
      - "25565:25565"
    environment:
      EULA: "TRUE"
      VERSION: "1.21.4"
      MEMORY: "4G"
      DIFFICULTY: "3"
      ENFORCE_SECURE_PROFILE: "false"
      TYPE: "FABRIC"
    volumes:
      - "./data:/data"
  playit:
    image: ghcr.io/playit-cloud/playit-agent:0.15
    restart: unless-stopped
    network_mode: "host"
    environment:
      - SECRET_KEY=${SECRET_KEY}
    stdin_open: true
    tty: true
    command: []